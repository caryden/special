# {{node-name}} → {{Language}}

- {{Type mapping: TypeScript type → target language type}}
- {{Data structure choice: e.g. "Use dataclass for options" or "Use struct with pub fields"}}
- {{Error handling: e.g. "Raise ValueError" or "Return Err()" or "Return error"}}
- {{Idiom: e.g. "Use list comprehension" or "Use iterator chain" or "Use slice"}}
- {{Testing: e.g. "Use pytest parametrize" or "Use #[test] with assert_relative_eq!"}}
- {{Any platform-specific notes}}
