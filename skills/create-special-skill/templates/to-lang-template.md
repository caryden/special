# {{node-name}} → {{Language}}

> Use this template only when a node has specific translation friction beyond
> what the skill-level `nodes/to-{{language}}.md` covers. If all guidance is
> already captured at the skill level, this file is unnecessary.

- {{Node-specific type mapping or data structure concern}}
- {{Node-specific idiom or pattern, e.g. "Box<> recursive children" or "circular buffer trim"}}
- {{Node-specific numerical or algorithmic concern}}
- {{Any platform-specific notes unique to this node}}
